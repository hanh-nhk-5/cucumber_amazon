Because the Amazon use CAPCHA to prevent automation run on their production site, so when script is run, for each scenario run, we need to input the CAPCHA manually but do not click the Confirm button 
